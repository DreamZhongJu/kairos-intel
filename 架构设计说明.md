# Kairós：架构设计说明

## 1. 项目目标与边界

### 1.1 项目目标

本项目是一个自托管的飞书个人研究助手。用户在飞书中提出问题、发送文件或给出明确操作指令后，助手能够按需检索公开网络、论文来源、飞书云文档、知识库与日程，并以中文给出可追溯的回答或执行受控的飞书写入操作。

核心目标：

1. 将“对话、检索、阅读、整理、归档”连接为一条可解释的工作流。
2. 由 LLM 根据用户意图自主选择工具；业务代码不依赖关键词匹配来决定是否联网或查知识库。
3. 保持私有数据可控：部署者自行持有模型密钥、飞书授权结果、记忆与日志。
4. 让失败可见、可恢复：搜索、模型或飞书接口失败时提供准确的降级说明，而不是编造内容。

### 1.2 非目标

1. 不在未得到明确指令时创建、修改、删除飞书文档、知识库节点或日程。
2. 文件上传事件只记录可供后续操作的上下文；不会因收到文件自动生成长回复或自动归档。
3. 不把所有聊天原文永久保存为长期记忆；长期记忆必须经过提取、去重和范围隔离。
4. 不把 API 密钥、OAuth 授权结果、个人文档内容、聊天记录或运行日志提交到 Git 仓库。

## 2. 用户入口与主要用例

唯一用户入口为飞书自建应用的私聊和群聊消息事件。群聊需要 @ 机器人或使用约定的触发方式，避免机器人误响应普通聊天。

主要用例如下：

| 用例 | 示例 | 期望结果 |
| --- | --- | --- |
| 联网研究 | “最近机器翻译有什么值得关注的进展？” | Agent 自主调用论文与网页检索，按可信度综合并附来源。 |
| 网页阅读 | “读这篇文章并写成笔记” | 读取用户提供的网址，提取正文，形成结构化摘要。 |
| 文件处理 | 先发送 PDF，再说“整理刚才的文件” | 从会话附件上下文找到文件，解析并总结；只有明确要求时才保存。 |
| 知识问答 | “我之前关于 RAG 的笔记结论是什么？” | 搜索已授权知识库、云文档和长期记忆，标明信息来源。 |
| 知识沉淀 | “把这份总结归档到科研知识库” | 先生成预览或确认目标，再创建云文档并归档到指定空间。 |
| 日程协助 | “我今天有什么安排？” | 读取授权日历并按时间排序。 |

## 3. 总体架构

```text
飞书事件
  -> Channel Adapter（验签、解析、去重、回复）
  -> Application Service（组装会话上下文、权限和任务）
  -> LangGraph Agent（模型决策、工具调用循环、结果整合）
  -> Tool Layer（网络、论文、飞书文档/知识库/日程、文件、记忆、MCP）
  -> Infrastructure（DeepSeek、Feishu OpenAPI、SQLite、Claude-Mem、HTTP）
```

依赖方向只能从上到下：`channels -> application -> agent -> tools -> infrastructure`。低层模块不能导入高层模块；例如网页搜索工具不能知道飞书消息格式，飞书适配器也不能直接拼接模型提示词。

## 4. 模块职责与接口

| 模块 | 职责 | 输入 | 输出 | 不负责什么 |
| --- | --- | --- | --- | --- |
| `channels` | 接收飞书事件、验签、消息解析、幂等去重、发送回复 | 飞书事件 | 标准化消息、发送结果 | Agent 决策、业务检索 |
| `application` | 构建请求上下文，协调权限、附件上下文与任务生命周期 | 标准化消息 | `AssistantRequest` | 直接调用底层 HTTP 或自行决定工具 |
| `agent` | 维护 LangGraph 状态，调用 LLM，决定工具，汇总最终答复 | 请求、上下文、可用工具 | `AssistantResponse` | 飞书协议细节、数据库 SQL |
| `tools` | 将外部能力封装为可调用工具（含 MCP 动态工具），并返回结构化结果 | 工具参数 | `ToolResult` | 对话人格、最终自然语言回答 |
| `memory` | 核心/存档两层记忆：提取、操作治理（add/update/delete）、召回、遗忘与预算 | 用户范围、查询或对话 | 相关记忆 | 代替知识库作为事实来源 |
| `observability` | 请求日志（工具链/token/耗时/状态）、统计与面板数据 | 请求上下文 | 指标行 | 用户意图判断 |
| `infrastructure` | 模型、HTTP、SQLite、飞书 OpenAPI、配置、日志等技术实现 | 明确参数 | 原始响应或领域对象 | 用户意图判断 |

### 4.1 核心领域对象

```text
AssistantRequest
  request_id, message_id, chat_id, user_id, text,
  attachments, referenced_urls, received_at, permission_scope

ToolResult
  tool_name, status, data, sources, error_code, retryable

AssistantResponse
  text, citations, actions, warnings
```

所有工具均应返回 `ToolResult`，即使失败也要有明确 `status`、`error_code` 与 `retryable`。Agent 根据结果决定重试、换工具或向用户说明限制。

## 5. LangGraph 工作流

LangGraph 是编排层，不是关键词路由器。Agent 节点将用户问题、会话摘要、相关长期记忆和可调用工具定义交给模型；模型以原生 tool calling 决定是否以及如何调用工具。

```text
START
  -> load_context
  -> agent
      -> tools  --（模型请求工具时）-->
      -> agent  --（工具结果回填后再次推理）-->
      -> finalize --（模型给出最终回答）-->
END
```

工作流约束：

1. 单次请求设置最大工具轮数，防止循环调用。
2. 高风险写工具（创建文档、知识库归档、创建日程）必须要求显式用户意图；批量操作需要预览或确认码。
3. 读取文件、网页或知识库的结果要截断、清洗并带来源，避免超出模型上下文。
4. 最终节点只输出面向用户的纯文本回复；不能暴露工具调用 XML、模型原始 JSON、异常栈或密钥。

## 6. 工具层设计

当前注册工具（`kairos/agent/runtime.py` 的 `NATIVE_TOOLS`，共 20 个）：

| 工具 | 类型 | 说明 |
| --- | --- | --- |
| `paper_lookup` / `huggingface_papers` | 只读 | 检索 arXiv、OpenAlex、Semantic Scholar、Hugging Face Papers。 |
| `web_search` / `read_webpage` | 只读 | 多来源网页搜索与正文读取。 |
| `read_feishu_document` / `knowledge_search` | 只读 | 读取飞书云文档；搜索已授权知识库。 |
| `daily_report` | 只读 | 读取每日情报日报。 |
| `today_schedule` | 只读 | 查询已授权飞书日程。 |
| `knowledge_save` / `save_cloud_document` | 写入 | 明确指令下写入知识库/创建云文档。 |
| `archive_to_knowledge_base` / `preview_cloud_archive` | 写入 | 明确指令下归档；批量归档先预览确认。 |
| `memory_search` | 只读 | 搜索当前用户范围内的长期记忆。 |
| `agent_reach_health` | 只读 | Agent-Reach 服务健康检查。 |
| `semantic_web_search` / `github_research` | 只读 | Exa 语义搜索；GitHub 仓库/活动检索。 |
| `youtube_video_details` / `x_search` / `reddit_search` / `bilibili_search` | 只读 | 平台内容检索（YouTube / X / Reddit / B 站）。 |

### MCP 动态工具

通过 `kairos/tools/mcp_client.py` 以 MCP（Model Context Protocol）动态接入外部工具：启动时读取 `mcp_servers.json`，自动发现每个 server 的工具并注册进 Agent（模型可见、可调用），无需为每个工具写代码。支持 `stdio`（本地进程）与 `streamable_http`（远程服务）两种传输；配置在启动时校验，无效 server 跳过并给出明确原因。

每个工具应定义：输入 schema、超时、最大重试次数、来源字段、权限前置条件和可否重试。工具实现不得自行调用 LLM；需要 LLM 判断的任务由 Agent 编排。

## 7. 记忆分层治理

长期记忆按 mem0 / Letta 的思路分两层治理（`kairos/memory/store.py`）：

| 层级 | 内容 | 加载方式 | 治理 |
| --- | --- | --- | --- |
| 核心记忆 | 用户长期身份与核心偏好（姓名、研究方向、重要偏好） | 每次请求总是加载 | 每用户上限 8 条，超出修剪最旧 |
| 存档记忆 | 项目、习惯、决定等可检索事实 | 按相关性召回 | 每用户上限 120 条，按最近访问时间清理 |

记忆写入由模型输出显式操作（`add` / `update` / `delete` / `noop`）：同一事实被合并更新而不是重复插入；用户说“忘记 XX”删除对应记忆；召回会更新访问统计（`last_accessed_at` / `access_count`）；敏感信息（密钥、密码）在写入前过滤。Claude-Mem 作为可选的对话级记忆仓库并行保留。

## 8. 可观测性与 Web 面板

每次请求自动记录到 `request_logs`（`kairos/observability/metrics.py`）：问题（脱敏）、工具调用链、prompt/completion token、耗时、状态与错误类型；owner 哈希存储。

本地 Web 面板（`web_panel.py`，Flask）：

- 仪表盘：请求量、成功率、今日失败、平均耗时、Token 消耗、估算成本、近 7 天趋势、工具调用排行（含失败数）、最近失败列表。
- 请求日志：按状态/工具筛选、分页、单条详情（工具链与完整问答）。
- 记忆：按用户查看核心/存档记忆、删除单条、清空用户记忆。
- 运行状态：模型、工具数、Claude-Mem、数据库路径等配置概览。

## 9. 评测

- 离线评测（`evaluation/run_eval.py`）：基于项目文档语料的知识问答（忠实度、回答相关性、hit@3、负例不编造率）与工具路由准确率，DeepSeek 作为生成与裁判模型，可复现。
- 真实请求回放（`evaluation/replay_eval.py`）：读取请求日志中的真实问答并打分（相关性、完整性），把生产使用转化为回归信号。
- CI（`.github/workflows/ci.yml`）：无凭据环境运行全部单测；配置 `DEEPSEEK_API_KEY` 时追加离线评测 job。

## 10. 数据、权限与安全

### 10.1 数据存储

| 数据 | 存储位置 | 保留原则 |
| --- | --- | --- |
| 幂等消息记录、任务状态 | SQLite | 设置过期清理，避免无限增长。 |
| 核心/存档记忆、访问统计 | SQLite | 按用户范围隔离，支持删除与预算清理。 |
| 请求日志（工具链/token/耗时） | SQLite | 脱敏存储；owner 哈希；question/answer 截断。 |
| 临时附件 | 本地临时目录或对象存储 | 处理完成后定期删除。 |
| 配置与授权结果 | `.env`、私有挂载卷 | 永不提交 Git。 |
| 运行日志 | 本地日志服务 | 脱敏并配置容量上限。 |

### 10.2 权限原则

1. 默认最小权限；读文档、写文档、知识库、日程分别授权。
2. 写入操作只在明确命令下执行，并记录操作者、目标和结果。
3. 知识库检索限定在已配置且应用有访问权的知识空间；不能把“无权限”伪装成“没有内容”。
4. 所有密钥从环境变量读取；示例文件只保留字段名。

## 11. 可靠性与降级策略

| 场景 | 策略 |
| --- | --- |
| 单个搜索源失败 | 记录失败，尝试备用来源；其他来源正常时继续回答。 |
| 所有联网来源失败 | 明确说明“本次未能验证实时信息”，可提供已有知识但必须标注其非实时性。 |
| 模型请求超时/失败 | 有限重试一次；仍失败时返回简短可理解提示，记录关联请求 ID。 |
| 飞书回复失败 | 保留待发送结果与请求 ID，允许安全重试；不重复创建文档或日程。 |
| 工具连续调用 | 设置最大轮数、总超时和重复调用检测，超过限制后结束并说明已完成部分。 |
| 写入工具失败 | 不自动无限重试；返回目标、失败原因和可安全重试的步骤。 |
| MCP server 不可用/配置无效 | 启动时跳过并记录明确原因，不影响内置工具。 |
| 记忆写入失败 | 不阻塞主流程（异步执行），记录异常。 |
| 观测层失败 | 日志写入失败不影响助手应答（fail-open）。 |
