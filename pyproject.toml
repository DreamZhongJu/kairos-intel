[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# Network, Feishu, and model SDKs expose a wide range of exception classes.
# Boundary adapters deliberately convert them into user-safe fallback messages.
ignore = ["BLE001"]
