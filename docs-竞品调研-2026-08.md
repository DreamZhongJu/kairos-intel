# 竞品调研：个人 AI 研究助手的特别功能

> 调研时间：2026-08-18。目标：为自托管、单用户、主打飞书、Python + LangGraph + SQLite 的个人助手「凯伊」做差异化功能对标。

## 各产品招牌功能与借鉴点

| 产品 | 招牌功能 | 对凯伊的借鉴点 |
| --- | --- | --- |
| **Khoj** | 定制 Agent（人格/按话题）、定时自动任务 Automations、多通道（Web/Obsidian/浏览器） | 定时订阅推送 + Agent 人格化 |
| **Open WebUI** | Pipes/Filters/Functions 三级扩展机制、多模型路由 | 统一的「技能=处理管道」抽象 |
| **gpt-researcher** | 多 Agent 流水线（规划→并行子检索→事实校验→带引用报告） | 子 Agent 并行检索 + 引用接地 |
| **LangBot** | 多平台消息网关（QQ/微信/飞书）、多模型路由 | 多平台适配层 + 多模型路由 |
| **Dify** | 可视化 Workflow + 知识管线、节点观测 | 每次回答记录 LangGraph 节点轨迹（可观测性） |
| **FastGPT** | Flow 可视化编排、知识库问答 | 把「召回→生成」建成可组合多级技能流 |
| **RAGFlow** | 深度文档解析（OCR/表格/公式）+ GraphRAG 图谱检索、证据引用 | **GraphRAG / 知识图谱 + 证据上抛** |
| **mem0 / Letta** | 抽取式长期记忆；Memory Blocks 上下文管理 | 借鉴 mem0「抽取式记忆」而非全量向量化 |
| **AnythingLLM** | 多工作区隔离 + 每次查询展示来源引用文件 | 引用接地（citation-grounded）+ 工作区隔离 |
| **n8n** | 可视化工作流 + 定时触发器 + 异步任务队列 | 定时触发 + 任务队列（自动研究/订阅） |
| **CogGPT** | 认知动力学/长期迭代 | 概念性启发（先粗后精的 agent 循环） |
| **DeepResearch / LangGraph** | 多 Agent 并行 + checkpoint 持久化 | 子图 / send 并行 / 任务续跑 |
| **Obsidian AI** | 语义搜索 + 相关笔记实时召回 | 聊到某话题时自动注入相关历史笔记 |

## 最值得优先做的 5 个功能点

1. **LangGraph 持久化 + 任务/会话堆栈** —— 让多步研究在重启后仍续跑（checkpoint 已用 SQLite，可深化为任务级）。
2. **引用接地流式回答** —— 每条输出带来源检索记录 → markdown 引用（**凯伊已实现**：参考来源脚注 + 流式分段）。
3. **定时订阅推送（夜间自动研究）** —— apscheduler + 飞书卡片推送，做成「每日/每周研究摘要」（**周报/月报已做**）。
4. **技能/插件注册表** —— 把能力抽象为可组合 skill，MCP 兼容（**技能读取接口 + skill_list/skill_load 已做**）。
5. **反馈闭环 + 小样本评测** —— 飞书 👍/👎 入库，定时运行回测改进 Prompt/检索策略（待做）。

## 结论

凯 Yi 已落地的（引用接地、流式、周报/月报、技能接口、知识图谱）正好覆盖调研中「引用接地、自动调度、技能体系、GraphRAG」等核心差异化功能。下一步可优先：**反馈闭环 + 小样本评测**、**多模型按任务/时延路由**、**定时订阅推送（用户级自动研究）**。